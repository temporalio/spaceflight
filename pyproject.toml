[project]
name = "spaceflight"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["temporalio", "pyserial"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
